import React from "react";
import { PestControlForm } from "../components";

const PestControl = () => {
  return (
    <div>
      <PestControlForm />
    </div>
  );
};

export default PestControl;
